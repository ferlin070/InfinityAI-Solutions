fastapi==0.136.1
uvicorn==0.46.0
openai==2.36.0
crewai==0.203.2
requests==2.33.1
python-dotenv==1.2.2
pydantic==2.13.4

# Testing
pytest==9.1.1
supabase==2.12.0
reportlab==4.3.1
apscheduler==3.11.0

# Optional AI provider bridges (install only what you need; the system
# gracefully reports the missing SDK at runtime if a tool is invoked).
# anthropic>=0.40.0        # Claude — set ANTHROPIC_API_KEY to use
# google-genai>=1.0.0      # Gemini — set GEMINI_API_KEY or GOOGLE_API_KEY to use
# playwright>=1.40.0       # Browser automation — also: `playwright install chromium`
# mcp>=1.0.0               # Model Context Protocol client
